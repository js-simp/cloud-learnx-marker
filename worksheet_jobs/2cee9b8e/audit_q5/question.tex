\documentclass[varwidth=17cm, border=12pt]{standalone}
\usepackage{amsmath, amssymb, xcolor, enumitem, tcolorbox}
\usepackage{varwidth}
\usepackage{tikz, pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{angles, quotes, calc, positioning, patterns, shapes.geometric}

% --- CONDITIONAL BORDER LOGIC ---
\newbool{drawborder}
\boolfalse{drawborder} % Default is OFF (for the cover)

\usepackage{background}
\usepackage{amssymb} % Required if using \square or \Box

\backgroundsetup{
  scale=1,
  angle=0,
  opacity=1,
  contents={
    \ifbool{drawborder}{% Only draw if the switch is ON
      \begin{tikzpicture}[remember picture,overlay]
        \draw [line width=1.5pt, color=gray!60, rounded corners=4mm] 
          ($(current page.north west) + (0.5in,-0.5in)$ ) 
          rectangle 
          ($(current page.south east) + (-0.5in,0.5in)$);
      \end{tikzpicture}
    }{} % Else draw nothing
  }
}

% ---- HEADER BOX ----------------------------------------
\newcommand{\firstpageheader}[1]{%
    \begin{center}
        \begin{tcolorbox}[
            colback=white,
            colframe=white,
            width=0.8\textwidth,
            boxrule=1pt,
            halign=center,
            top=3mm, bottom=3mm
        ]
            \textbf{Answer ALL #1 questions.} \\[1mm]
            \textbf{Write your answers in the spaces provided.} \\[1mm]
            \textbf{You must write down all the stages in your working.}
        \end{tcolorbox}
    \end{center}
    \vspace{4mm}
}

% Continuous exam box with internal dividers
\newcommand{\examboxes}[1]{%
\begin{tikzpicture}[baseline=(current bounding box.center)]
    % dimensions
    \def\boxwidth{0.9}   % width of each small box
    \def\boxheight{1}  % height
    \def\totalwidth{#1*\boxwidth}
    
    % outer rounded rectangle
    \draw[line width=2pt, rounded corners=3pt, color=black!60]
        (0,0) rectangle (\totalwidth,\boxheight);
    
    % internal vertical lines
    \foreach \i in {1,...,#1} {
        \ifnum\i<#1
            \draw[line width=2pt, color=black!60]
            (\i*\boxwidth,0) -- (\i*\boxwidth,\boxheight);
        \fi
    }
\end{tikzpicture}
}


% --- Custom Question Environment ---
\newenvironment{question}{%
    \refstepcounter{questionNum}
    \setcounter{questionmarks}{0}
    \vspace{4mm}
    \begin{list}{}%
    {
        \setlength{\leftmargin}{0.5cm}
        \setlength{\labelwidth}{1.2cm}
        \setlength{\labelsep}{0.2cm}
        \setlength{\itemindent}{0cm}
        \setlength{\listparindent}{0cm}
        \setlength{\topsep}{0pt}
        \setlength{\partopsep}{0pt}
    }
    \item[\textbf{\thequestionNum}]
}{%
    \end{list}
    \vspace{2mm}
    \noindent\hfill
    \textbf{(Total for Question \thequestionNum\ is \thequestionmarks\ marks)}
    \par\vspace{3mm}
    \hrule
}

% Command to add parts of questions
\newcommand{\partq}[1]{%
    \vspace{3mm}
    \par
    \hangindent=1cm
    \hangafter=1
    \makebox[1cm][l]{(#1)}
}

% answer marks
\newlength{\answerwidth}
\setlength{\answerwidth}{3cm}
% Core right-aligned answer block
\newcommand{\answerbox}[3]{%
    % #1 = prefix
    % #2 = suffix
    % #3 = marks
    \vspace{2mm}
    \noindent\hfill
    \begin{minipage}{4.5cm}
        \raggedleft
        #1\makebox[\answerwidth]{\dotfill}#2
        \vspace{1mm}
        
        \hfill\makebox[\answerwidth][c]{\textbf{(#3)}}
    \end{minipage}
    \addtocounter{questionmarks}{#3}
    \addtocounter{totalmarks}{#3}
}

% 1) Just dotted line
\newcommand{\answerplain}[1]{%
    \answerbox{}{}{#1}
}

% 2) Dotted line + unit after
\newcommand{\answerunit}[2]{%
    \answerbox{}{\,#1}{#2}
}

% 3) Unit before dotted line
\newcommand{\answerprefix}[2]{%
    \answerbox{#1\,}{}{#2}
}

% 4) Variable before dotted line (e.g. x = )
\newcommand{\answereq}[2]{%
    \answerbox{$#1 =$\,}{}{#2}
}

% 5) Variable + unit
\newcommand{\answerequnit}[3]{%
    \answerbox{$#1 =$\,}{\,#2}{#3}
}

% Explanation / reasoning space
\newcommand{\answerlines}[2]{%
    % #1 = number of lines
    % #2 = marks
    
    \vspace{2mm}
    
    \noindent
    \begin{minipage}{\linewidth}
        \foreach \i in {1,...,#1} {
            \noindent\hdashrule[0.5ex]{\linewidth}{0.5pt}{2pt}\\[3mm]
        }
        
        \vspace{1mm}
        \noindent\hfill\textbf{(#2)}
    \end{minipage}
    
    \addtocounter{questionmarks}{#2}
    \addtocounter{totalmarks}{#2}
}

% Coordinate answer box
\newcommand{\answercoord}[1]{%
    \vspace{2mm}
    \noindent\hfill
    \begin{minipage}{4.5cm}
        \centering
        $\left(\makebox[1.5cm]{\dotfill},\;\makebox[1.5cm]{\dotfill}\right)$
        \vspace{1mm}
        
        \makebox[3cm][c]{\textbf{(#1)}}
    \end{minipage}
    \addtocounter{questionmarks}{#1}
    \addtocounter{totalmarks}{#1}
}

% Marks only (no dotted line)
\newcommand{\answermarks}[1]{%
    \vspace{2mm}
    \noindent\hfill
    \textbf{(#1)}
    \addtocounter{questionmarks}{#1}
    \addtocounter{totalmarks}{#1}
}

% Macro for 4-option MCQ
\newcommand{\answermcq}[5]{
    \vspace{0.5cm}
    \begin{center}
        \renewcommand{\arraystretch}{1.5}
        \begin{tabular*}{0.8\textwidth}{@{\extracolsep{\fill}} c c c c }
            #1 & #2 & #3 & #4 \\
            \framebox[15pt]{\rule{0pt}{15pt}} & \framebox[15pt]{\rule{0pt}{15pt}} & \framebox[15pt]{\rule{0pt}{15pt}} & \framebox[15pt]{\rule{0pt}{15pt}} 
        \end{tabular*}
    \end{center}
    \vspace{0.25cm}
    \answermarks{#5}
}

\newcommand{\paperTotal}{%
    \vspace{10mm}
    {\color{gray}\hrule height 3pt} % Soft gray rule to match the specimen
    \vspace{1.5mm}
    \noindent\hfill
    \textbf{TOTAL FOR PAPER IS \total{totalmarks}\ MARKS}
}

\begin{document}
\begin{varwidth}{17cm}
\begin{question}
\noindent A space engineer is modelling the signal range of a satellite dish. The circular boundary of the signal, centre $O$, has two straight-line signal paths $PA$ and $PB$ drawn from the receiver at point $P$ (outside the circle) to points $A$ and $B$ on the edge of the boundary. Each signal path just touches the circle, so $PA$ and $PB$ are tangents to the circle at $A$ and $B$ respectively.

Angle $APB = 46^\circ$.

\begin{center}
\begin{tikzpicture}[scale=2]
    % Define the center of the circle
    \coordinate (O) at (0,0);

    % Draw the circle
    \draw[fill=lightgray!30] (O) circle (1cm);
    \fill[black] (O) circle (0.5pt);
    \node at (0.15,0.05) {$O$};

    % Place points A and B on the circumference
    \coordinate (A) at ($(O) + (113:1cm)$);
    \coordinate (B) at ($(O) + (247:1cm)$);

    % Calculate tangent directions and find external point P
    \coordinate (T1) at ($(A)!4!-90:(O)$);
    \coordinate (T2) at ($(B)!4!90:(O)$);

    \path [name path=line1] (A) -- (T1);
    \path [name path=line2] (B) -- (T2);
    \path [name intersections={of=line1 and line2,by={P}}];

    % Draw tangent lines
    \draw (A) -- (P) -- (B);

    % Draw radii
    \draw (O) -- (A);
    \draw (O) -- (B);

    % Labels
    \node at (A) [above right] {$A$};
    \node at (B) [below right] {$B$};
    \node at (P) [right] {$P$};

    % Mark right angles
    \tkzMarkRightAngle[size=.2](O,A,P);
    \tkzMarkRightAngle[size=.2](P,B,O);

    % Mark the angle at P
    \pic [draw, angle eccentricity=1.4, angle radius = 0.4cm, "$46^\circ$"] {angle = A--P--B};

\end{tikzpicture}
\end{center}

Find the size of angle $AOB$, the angle at the centre of the circle between the two radii $OA$ and $OB$.

\vspace{5cm}

\begin{flushright}
\answerequnit{AOB}{$^\circ$}{4}
\end{flushright}
\end{question}

\end{varwidth}
\end{document}
