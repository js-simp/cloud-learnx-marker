\documentclass{article}
\usepackage[active,tightpage]{preview}
\setlength\PreviewBorder{12pt}

% ===============================
%   PROJECT-SPECIFIC ENVIRONMENT
% ===============================
\input{layout/packages}
\input{config}
\input{layout/macros}

\begin{document}
\begin{preview}
\begin{question}
\noindent Quadrilateral $PQRS$ is shown on the grid below.

\begin{center}
\begin{tikzpicture}[scale=0.55, transform shape]
    \def\axisLimit{8}

    \definecolor{borderColour}{HTML}{000000}
    \definecolor{fillColour}{HTML}{D8D8D8}

    % Draw grid
    \draw[step=1cm,gray,very thin] (-\axisLimit,-\axisLimit) grid (\axisLimit,\axisLimit);

    % Draw axes
    \draw[thick,->] (-\axisLimit-0.5,0) -- (\axisLimit+0.5,0) node[anchor=north] {$x$};
    \draw[thick,->] (0,-\axisLimit-0.5) -- (0,\axisLimit+0.5) node[anchor=east] {$y$};

    % Label x-axis and y-axis
    \foreach \x in {-8,...,-1,1,2,...,8}
        \draw (\x cm,1pt) -- (\x cm,-1pt) node[anchor=north] {\x};
    \foreach \y in {-8,...,-1,1,2,...,8}
        \draw (1pt,\y cm) -- (-1pt,\y cm) node[anchor=east] {\y};

    % Centre of enlargement
    \coordinate (COE) at (0,0);
    \draw[red, thick] (COE) node[cross out, draw=red, inner sep=3pt, outer sep=0pt, line width=1.2pt] {};
    \node[below left] at (0,0) {$O$};

    % Define the vertices for quadrilateral PQRS
    \coordinate (P) at (2,4);
    \coordinate (Q) at (6,4);
    \coordinate (R) at (6,2);
    \coordinate (S) at (2,-2);

    % Draw the quadrilateral
    \draw[fill=fillColour, draw=borderColour, line width=1pt] (P) -- (Q) -- (R) -- (S) -- cycle;

    \node[above] at (P) {$P$};
    \node[above] at (Q) {$Q$};
    \node[right] at (R) {$R$};
    \node[below] at (S) {$S$};

\end{tikzpicture}
\end{center}

\noindent Quadrilateral $PQRS$ has vertices $P(2,4)$, $Q(6,4)$, $R(6,2)$ and $S(2,-2)$.

\noindent Enlarge $PQRS$ by scale factor $\dfrac{1}{2}$, centre $(0,0)$.

\noindent Write down the coordinates of the image of each vertex.

\vspace{4cm}

\begin{flushright}
    \answerplain{3}
\end{flushright}
\end{question}

\end{preview}
\end{document}
