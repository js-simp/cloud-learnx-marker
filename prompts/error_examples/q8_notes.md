# Q8 — Review Notes

## Error classes identified
- MATH CONTENT ERROR
- FLOATING LABELS AND ALIGNMENT
- OTHER

## Explanation of errors and fixes
1) Centre of rotation marked is not the correction value, and more importantly even if it was it shouldn't be marked on the grid, as the problem requires the student to describe the transformation. Fixed by removing the COR.
2) Minor error : Labels for Shape A and B are at the edge, should be at the center of the shape. Fixed by adjusting the coordinates.
3) vspace added for a question that has answerlines. Fixed by removing the vspace added.
4) Question doesn't have a unique solution (could either be a reflection or a rotation. Fixed by labeling the vertices of the shape and the corresponding vertices of the image.
